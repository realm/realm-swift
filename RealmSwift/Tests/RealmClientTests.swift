import Foundation
import RealmSwift
import XCTest

class RealmClientTests: XCTestCase {

    func testClient() throws {
//        let app = RealmApp.app(appId: "translate-utwuv")
//
//        var exp = expectation(description: "should login")
//        app.auth.logIn(with: SyncCredentials.anonymous(), onCompletion: { user, error in
//            if user == nil {
//                XCTFail(error?.localizedDescription ?? "unknown failure")
//            }
//            exp.fulfill()
//        })
//        wait(for: [exp], timeout: 30)
//
//        exp = expectation(description: "should translate")
//        app.functions.translate([
//            ["lang":"en-fr",
//             "text": "Hey, how are you doing?"]
//        ]) { (result: Result<Translation, Error>) in
//            switch result {
//            case .success(let translation):
//                XCTAssertNotNil(translation)
//            case .failure(let error):
//                XCTFail(error.localizedDescription)
//            }
//            exp.fulfill()
//        }
//        wait(for: [exp], timeout: 30)
    }
}
